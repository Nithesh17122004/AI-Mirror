# Self-hosted VTON inference server — dependencies
# Target: NVIDIA GPU + CUDA (Ampere or newer) machine. See README.md.

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Image handling
Pillow>=10.0

# HTTP client for remote garment images
requests>=2.31

# FASHN VTON v1.5 inference code (official GitHub repo, Apache-2.0).
# Pulls the pipeline + scripts. Requires torch/onnxruntime-gpu below.
fashn-vton @ git+https://github.com/fashn-AI/fashn-vton-1.5.git@main

# Deep learning runtime (GPU). The fashn-vton package pins its own torch;
# if conflicts arise, align these with the official repository's pyproject.
torch>=2.4
onnxruntime-gpu>=1.17